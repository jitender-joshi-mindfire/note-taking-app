# specs/

Current system state — the source of truth for what's actually built, by capability
(`specs/<capability>/spec.md`). Empty until the first change is archived.

Populated automatically by `openspec archive <change-name>`, which merges an approved change's
spec deltas into these files. Never hand-edit a file here directly — change it via a new
`/spec` proposal instead.
